{
  "name": "zzzbees",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling for zzzbees.com. The site itself is plain HTML and CSS in site/.",
  "scripts": {
    "images": "node scripts/images.mjs",
    "test": "node scripts/check.mjs",
    "serve": "python3 -m http.server 8000 --directory site"
  },
  "devDependencies": {
    "chrome-launcher": "^1.1.2",
    "lighthouse": "^12.2.1",
    "sharp": "^0.33.5"
  }
}
